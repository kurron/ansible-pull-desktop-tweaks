set -o vi

# from https://codeburst.io/7-super-useful-aliases-to-make-your-development-life-easier-fef1ee7f9b73
alias nis="npm install --save "
alias gac="git add . && git commit -a -m "
alias hs='history | grep'
alias mkcd='foo(){ mkdir -p "$1"; cd "$1" }; foo '
alias myip="curl http://ipecho.net/plain; echo"
alias svim='sudo vim'
alias update='sudo apt-get update && sudo apt-get upgrade'
alias show-git-email='git config user.email'
alias set-git-email='git config user.email "kurr@jvmguy.com"'
alias aws='docker run --rm --interactive --tty --volume ~/.aws:/root/.aws --volume $(pwd):/aws amazon/aws-cli:latest'

